{
  "name": "rat_race",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:purescript": "spago build",
    "build": "pnpm run build:purescript && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^4.9.3",
    "vite": "^4.1.0"
  },
  "dependencies": {
    "concurrently": "^7.6.0",
    "purescript": "0.15.4",
    "spago": "^0.20.9",
    "tailwindcss": "^3.2.4"
  }
}
